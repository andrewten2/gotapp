import React from 'react';
import { Alert } from 'reactstrap';

const ErrorMessage = () =>{
    return  <Alert color="danger" align="center">We have some problems!</Alert>
}

export default ErrorMessage;